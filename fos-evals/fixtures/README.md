# Fixtures

One JSON file per agent scenario: input records + expected output-schema constraints + expected deterministic-gate outcomes. Populated with the first agent slice (Phase 1). Kept in-repo (synthetic only — never real applicant data).
